This folder is intentionally empty in source control.

The application creates products.json, stock_movements.json and
sales.json here automatically the first time it runs (see
Data/DataService.cs). A small set of demo products is seeded
automatically the very first time the app starts with no data files
present, so the app is ready to demo immediately after opening it.