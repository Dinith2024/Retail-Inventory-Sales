<UserControl x:Class="RetailInventorySales.Views.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="360"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="280"/>
            </Grid.RowDefinitions>

            <!-- Current stock levels -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Current Stock Levels" Style="{StaticResource SectionHeader}"/>
                    <DataGrid x:Name="StockGrid" SelectionChanged="StockGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="90"/>
                            <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="In Stock" Binding="{Binding QuantityInStock}" Width="80"/>
                            <DataGridTextColumn Header="Reorder At" Binding="{Binding ReorderLevel}" Width="90"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding StockStatus}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- Recent movements -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Recent Stock Movements" Style="{StaticResource SectionHeader}"/>
                    <DataGrid x:Name="MovementsGrid">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date/Time" Binding="{Binding Timestamp, StringFormat='dd MMM yyyy HH:mm'}" Width="150"/>
                            <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="90"/>
                            <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding MovementType}" Width="110"/>
                            <DataGridTextColumn Header="Change" Binding="{Binding QuantityChange}" Width="70"/>
                            <DataGridTextColumn Header="Resulting Stock" Binding="{Binding ResultingStock}" Width="110"/>
                            <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Adjustment form -->
        <Border Grid.Column="2" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Adjust Stock" Style="{StaticResource SectionHeader}"/>

                <TextBlock Text="Product" Style="{StaticResource LabelText}"/>
                <ComboBox x:Name="ProductCombo" DisplayMemberPath="DisplayLabel" Margin="0,0,0,10" SelectionChanged="ProductCombo_SelectionChanged"/>

                <TextBlock x:Name="CurrentStockText" Text="Current stock: -" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,10"/>

                <TextBlock Text="Movement Type" Style="{StaticResource LabelText}"/>
                <ComboBox x:Name="MovementTypeCombo" Margin="0,0,0,10" SelectedIndex="0">
                    <ComboBoxItem Content="Restock (goods received)" Tag="Restock"/>
                    <ComboBoxItem Content="Manual Adjustment / Correction" Tag="ManualAdjustment"/>
                    <ComboBoxItem Content="Customer Return" Tag="Return"/>
                </ComboBox>

                <TextBlock Text="Quantity" Style="{StaticResource LabelText}"/>
                <TextBox x:Name="QuantityBox" Margin="0,0,0,4"/>
                <TextBlock Text="Enter a positive number to increase stock, or a negative number to decrease it (e.g. -3 for damaged goods)."
                           TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,10"/>

                <TextBlock Text="Notes" Style="{StaticResource LabelText}"/>
                <TextBox x:Name="NotesBox" Margin="0,0,0,10" AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>

                <TextBlock x:Name="ValidationText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed"/>

                <Button Content="Apply Adjustment" Style="{StaticResource PrimaryButton}" Click="ApplyAdjustment_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
