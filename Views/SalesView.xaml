<UserControl x:Class="RetailInventorySales.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- Product picker + cart -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Add Product to Sale" Style="{StaticResource SectionHeader}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="160"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Product" Style="{StaticResource LabelText}"/>
                            <ComboBox x:Name="ProductCombo" DisplayMemberPath="DisplayLabel" SelectionChanged="ProductCombo_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Quantity" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="QuantityBox" Text="1"/>
                        </StackPanel>

                        <StackPanel Grid.Column="4" VerticalAlignment="Bottom">
                            <Button Content="Add to Cart" Style="{StaticResource PrimaryButton}" Click="AddToCart_Click"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock x:Name="StockHintText" Margin="0,8,0,0" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                    <TextBlock x:Name="AddErrorText" Margin="0,4,0,0" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Cart" Style="{StaticResource SectionHeader}"/>
                    <DataGrid x:Name="CartGrid" IsReadOnly="False" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="90" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat='Rs. {0:N2}'}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="70" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat='Rs. {0:N2}'}" Width="110" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Remove" Style="{StaticResource SecondaryButton}" Padding="8,4" Click="RemoveFromCart_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Sale summary -->
        <Border Grid.Column="2" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Complete Sale" Style="{StaticResource SectionHeader}"/>

                <TextBlock Text="Payment Method" Style="{StaticResource LabelText}"/>
                <ComboBox x:Name="PaymentCombo" Margin="0,0,0,10" SelectedIndex="0">
                    <ComboBoxItem Content="Cash"/>
                    <ComboBoxItem Content="Card"/>
                    <ComboBoxItem Content="Mobile Wallet"/>
                </ComboBox>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Discount %" Style="{StaticResource LabelText}"/>
                        <TextBox x:Name="DiscountBox" Text="0" TextChanged="AdjustmentBox_TextChanged"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Tax %" Style="{StaticResource LabelText}"/>
                        <TextBox x:Name="TaxBox" Text="0" TextChanged="AdjustmentBox_TextChanged"/>
                    </StackPanel>
                </Grid>

                <Separator Margin="0,4,0,12"/>

                <Grid Margin="0,0,0,6">
                    <TextBlock Text="Subtotal" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="SubtotalText" Text="Rs. 0.00" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="0,0,0,6">
                    <TextBlock Text="Discount" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="DiscountAmountText" Text="- Rs. 0.00" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="0,0,0,6">
                    <TextBlock Text="Tax" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="TaxAmountText" Text="+ Rs. 0.00" HorizontalAlignment="Right"/>
                </Grid>
                <Separator Margin="0,4,0,12"/>
                <Grid Margin="0,0,0,20">
                    <TextBlock Text="TOTAL" FontWeight="Bold" FontSize="16" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="TotalText" Text="Rs. 0.00" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryBrush}"/>
                </Grid>

                <TextBlock x:Name="SaleErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,10" Visibility="Collapsed"/>

                <Button Content="Complete Sale" Style="{StaticResource PrimaryButton}" Click="CompleteSale_Click" Margin="0,0,0,8"/>
                <Button Content="Clear Cart" Style="{StaticResource SecondaryButton}" Click="ClearCart_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
